name: VEXTONY-Brain-Robot
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Start Processing
        run: echo "VEXTONY Brain is now thinking..."
